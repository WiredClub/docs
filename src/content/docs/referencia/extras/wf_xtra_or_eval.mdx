---
title: "WIRED EXTRA: Pelo menos uma condição é verdadeira"
description: Este extra faz a pilha continuar sua execução se a quantidade de condições configuradas nele for verdadeira.
sidebar:
  label: Pelo menos uma condição é verdadeira
  badge:
    text: RASCUNHO
    variant: note
infobox:
  # == Infobox ==
  # hide: false # valor padrão
  title: Pelo menos uma condição é verdadeira
  # image: # url preenchida automaticamente

  # == Dados do Mobi ==
  revision: 69610
  classname: wf_xtra_or_eval
  name: "WIRED EXTRA: Pelo menos uma condição é verdadeira"
  description: Este extra faz a pilha continuar sua execução se a quantidade de condições configuradas nele for verdadeira.
  # product_name: # caso o nome na loja seja diferente do nome do Mobi
  # image_direction: 0 # valor padrão
  # image_not_animated: false # valor padrão
  # image_animated_state: 100 # valor padrão
  # icon: # url preenchida automaticamente
  availability: No catálogo e CA
  price: 5
  release_date: 2022-08-29

  # == Dados do Wired ==
  type: Extra
  # requires_bot: false # valor padrão
  requires_furni: true
  # requires_antena: false # valor padrão
  # requires_contract: false # valor padrão
  # requires_chest: false # valor padrão
  # execution_limit: 100 # valor padrão
  # execution_limit_per_user: 100 # valor padrão

  # == Dados do Extra ==
  additional_sources: []
---

import Infobox from "@components/Infobox.astro"

<Infobox />
